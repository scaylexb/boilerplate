/**
 * Adds additional DOM matchers to the Vitest testing environment.
 * Used in test setup to enable jest-dom matchers like `toBeInTheDocument()` and `toHaveClass()`.
 *
 * @see https://github.com/testing-library/jest-dom
 */
import '@testing-library/jest-dom/vitest'
